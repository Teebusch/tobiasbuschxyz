<div class="center-wrapper">
    <div class="center">
        <slot />
    </div>
    <aside>
        <small class="aside-content">
            <slot name="aside" />
        </small>
    </aside>
</div>

<style>
    .center-wrapper {
        width: 100%;
        display: grid;
        grid-template-areas: 'left center right';
        grid-template-columns: minmax(0, 1fr) min(900px, 100%) 1fr;
        gap: var(--space-500);
        align-items: start;
    }
    .center {
        grid-area: center;
        box-sizing: border-box;
        width: 100%;
        margin-inline: auto;
    }
    aside {
        grid-area: right;
        justify-self: start;
        align-self: start;
        padding-block: var(--space-400);

        .aside-content {
            font-size: var(--font-size--1);
            color: var(--color-text-secondary);
            min-width: 250px;
            max-width: 250px;
            line-height: 1.7;
            letter-spacing: 0.05em;
        }
    }
    @media (max-width: 1024px) {
        .center-wrapper {
            grid-template-areas:
                'center'
                'right';
            grid-template-columns: 1fr;
            gap: 0;
        }
        .center {
            padding-inline: var(--space-400);
        }
        aside {
            position: static;
            justify-self: stretch;
            padding-block: 0;
            padding-inline: var(--space-400);
            margin-block: 0 var(--space-400);
        }
    }
</style>
